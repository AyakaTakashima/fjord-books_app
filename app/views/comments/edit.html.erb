<h1><%= t('views.common.edit') %></h1>

<%= form_with(model: [@commentable, @comment]) do |f| %>
  <div class="field">
    <%= f.text_area :content %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to t('views.common.back'), @commentable %>
